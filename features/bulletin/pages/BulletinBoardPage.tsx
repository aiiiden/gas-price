import { NextPage } from "next";
import BulletinBoard from "../components/BulletinBoard";

const GasPage: NextPage = () => {
    return (
        <div>
            <BulletinBoard />
        </div>
    );
};

export default GasPage;
